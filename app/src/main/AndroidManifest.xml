<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lyj.permissions">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Transparent.LYJPermission" >
        <activity
            android:name=".LYJPermissionAct"
            android:configChanges="screenSize|keyboard|keyboardHidden|mnc|mcc|smallestScreenSize|screenLayout|uiMode|fontScale|touchscreen|orientation">
        </activity>
    </application>

</manifest>
